import React from 'react';
import {Document} from 'react-sketchapp';

import UIKitPage from '/component/page/UIKitPage';

const Root = () => (
  <Document>
    <UIKitPage/>
  </Document>
);

export default Root;
